{
  "name": "neos-rfid-system",
  "version": "6.0.0",
  "scripts": {
    "dev": "firebase serve",
    "deploy": "firebase deploy --only hosting",
    "build": "dotnet publish src/gateway/Rfid_gateway.csproj -c Release -o dist/gateway",
    "test": "jest",
    "test:rules": "jest tests/firestore.rules.test.ts --verbose"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "firebase": "^11.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
